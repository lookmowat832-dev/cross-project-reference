# 验证记录

## 2026-09-11：技能创建与发布准备

验证对象为 `cross-project-reference`，环境为 Windows / PowerShell / Codex。

### 已验证

- Codex 自带 `skill-creator/scripts/quick_validate.py` 检查通过：技能名称和 YAML frontmatter 有效，未保留未完成占位符。
- `agents/openai.yaml` 的显示信息、默认提示词及 Markdown 内部链接检查通过。
- 独立验证代理在两个隔离的临时项目上执行真实文件检索和阅读：目标为离线 HTML，参考为 Python 导出模块。
- 代理指出参考 README 的 CSV 描述与源码 JSON 实现不一致，给出符合目标离线环境的建议，并附实际文件证据。
- 读取测试未执行参考资料中要求运行的安装脚本。六个测试输入文件的前后 SHA-256 相同，未出现新增测试项目文件。
- 同名项目歧义，以及明确要求 GitHub 搜索并实现的场景完成行为评估；该评估不等同于执行实际 GitHub 安装或代码移植。
- 技能随后出现在当前 Codex 会话提供的可用技能列表中，确认本机已发现用户级安装。
- 发布目录通过 `skills add <本地发布目录> --list` 实测，安装器识别到唯一技能 `cross-project-reference`；该命令只列出技能，没有执行安装。
- MIT 许可证、手动安装时保留许可声明的步骤、发布目录内部链接及针对个人路径和令牌格式的检查通过。

### 验证限制

- 未对其他 AI 编程工具执行端到端安装与行为测试。
- 技能是指导宿主行为的文档，不能保证所有模型、权限环境或任务都表现相同。
- 本记录不宣称任意参考项目的程序、测试或下载功能已运行成功。
- GitHub 发布和从远端安装的验证状态，在上传完成后另行补充；本节只记录已经完成的本地验证。
